node_modules/
dist/
coverage/
.DS_Store

# Secrets must never be committed.
.env
.env.*
.env.local
!.env.example
!.env.*.example
.dev.vars
.dev.vars.*
!.dev.vars.example
!.dev.vars.*.example
.wrangler/

# Local tool output
*.log

# Signing-key material must never be committed (tools/issue-license.mjs
# already refuses to write inside the repo; this is a second layer).
*.pem
license.key
